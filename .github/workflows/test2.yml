name: 매일 브리핑 테스트

on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: "발행하지 않고 로그만 남기기"
        type: boolean
        default: false

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - run: echo "hello ${{ inputs.dry_run }}"
